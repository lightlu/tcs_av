ffmpeg
320x240

主要參數[編輯]
-i設定輸入檔名。
-f設定輸出格式。
-y若輸出檔案已存在時則覆蓋檔案。
-ss從指定時間開始轉換。
-title設定標題。

影像參數[編輯]
-s設定畫面的寬與高。
-vcodec設定影像影像編解碼器，未設定時則使用與輸入檔案相同之編解碼器。


FFmpeg 也可以用來切割影片， -ss 是指定從哪裡開始切， -t 是從 -ss 開始切幾秒。如： 
ffmpeg -i i.ts -ss 00:05:00 -t 00:55:00 o.ts
